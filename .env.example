# URL of the deployed backend (see server/), used server-side by app/api/leaderboard/route.ts.
# Local dev default: http://localhost:8080 (run `npm run dev` in server/).
# Production: your Render service URL, e.g. https://leaderboard-backend.onrender.com
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
